"use strict";

/**
 * Ingest folder skeleton (Phase 1) — TikFinity / Kick adapters land here later.
 * Live ingest still enters via index.js → event pipeline.
 */

module.exports = {
  phase: 1,
  status: "stub",
  note: "Adapters not extracted yet — use shared/platform_normalizers + event pipeline."
};
