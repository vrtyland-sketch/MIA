"use strict";

/**
 * Flatten grouped interpreter host bindings for MIA_TRANSLATE.createTranslationRuntime.
 */

function buildInterpreterCtx(_host = {}) {
  return {};
}

module.exports = { buildInterpreterCtx };
