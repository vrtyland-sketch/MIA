"use strict";

/**
 * Assemble grouped interpreter host bindings from flat index bindings.
 */

function buildInterpreterHost(_bindings = {}) {
  return {};
}

module.exports = { buildInterpreterHost };
