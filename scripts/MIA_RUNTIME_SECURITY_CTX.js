"use strict";

/**
 * Flatten grouped runtime security guard host bindings (factories take no deps today).
 */

function buildRuntimeSecurityCtx(_host = {}) {
  return {};
}

module.exports = { buildRuntimeSecurityCtx };
