# MIA — audit projektu (18. 6. 2026)

> Git na tomto PC není v PATH — **větve git nešly zkontrolovat**. Audit je z mapy souborů + `require()` grafu.

---

## 1. Co je PRODUKCE (nechat, dokončovat tady)

Jediná cesta streamu:

```
TikFinity/Kick → index.js → normalize → shadow pipeline (MIA_NEXT)
  → decision (shared) → action builder → overlay/TTS/video/OBS
```

| Vrstva | Soubory | Stav |
|--------|---------|------|
| **Entry** | `index.js` | ✅ aktivní |
| **Pipeline** | `MIA_NEXT/engine_shadow_runtime.js`, `engine_spam_session.js` | ✅ aktivní |
| **Rozhodování** | `shared/platform_runtime_rules/decision_engine.js` | ✅ aktivní |
| **Akce** | `shared/platform_runtime/action_builder.js` | ✅ aktivní |
| **Support** | `scripts/MIA_SUPPORT_RESOLVER.js` (`legacy/` shim) | ✅ aktivní |
| **OBS overlaye** | split HTML (`speech`, `bowl`, `runtime`, `voice`, `story`) | ✅ doporučeno |
| **Video gifty** | `MIA_VIDEO_ENGINE.js` | ✅ aktivní |
| **Koj stav** | bowl, vitals, evolution, backpack, care | ✅ aktivní |
| **TTS** | Edge (zdarma) | ✅ aktivní |
| **Solo stream** | `MIA_PROACTIVE_HOST` + `MIA_SOLO_STREAM` + OBS scény | ✅ nové, dokončit OBS názvy scén |
| **Story** | `MIA_STORY_VIDEO_ENGINE` (OBS videa) + PNG fallback | ✅ video = default |
| **MIA oči** | `MIA_EYES.js` | ✅ aktivní |
| **Kanon Koj** | `moods/*.png` + `restore:koj-sprites` | ✅ jediná správná grafika |

**Proč nechat:** bez toho stream neběží. Vše ostatní je obal nebo experiment.

---

## 2. ROZDELANÉ / DUplicitní větve (priorita)

### 🔴 Odstranit nebo archivovat (mrtvé, ~37 MB + ~25 souborů)

| Položka | Problém | Doporučení |
|---------|---------|------------|
| **`mega/` (405 PNG, ~37 MB)** | Procedurální banka, runtime **nenačítá** | **Smazat / archiv mimo repo** |
| **`MIA_KOJNOZROUT_MEGA_BANK.js`** | Jen test, ne index | Archiv |
| **`kojnozrout_generate_mega_bank.js`** | Generuje mega | Archiv |
| **`kojnozrout_generate_full/base_sprites.js`** | Procedurální blob ≠ Koj | Archiv |
| **Ingest chain** (`MIA_INGEST_ROUTER`, event bus, classifiers…) | Rozbité cesty, nepoužívá index | Archiv |
| **`legacy/` kromě SUPPORT_RESOLVER** | Duplicita shared pipeline | Archiv |
| **`MIA_NEXT/action`, `decision`, wrappers, runtime_switch** | Fork — aktivní je `shared/` | Archiv |
| **`MIA_GREETING_AGGREGATOR` + `shared/aggregate`** | Jen test, není ve streamu | Archiv nebo **dodělat** |
| **`mia-live-hub.html`** | Legacy all-in-one, index říká „nedoporučeno“ | Držet redirect, OBS = split |

### 🟡 Nechat, ale sjednotit (čas to dodělat)

| Položka | Co chybí | Effort |
|---------|----------|--------|
| **Kontrakty `MIA_NEXT/core_contracts_*`** vs `shared/platform_runtime_contracts/` | Dva zdroje pravdy (share bridge používá MIA_NEXT) | Střední — sloučit |
| **Story PNG vs video** | Oba režimy existují | OK — default `obs_video`, PNG = fallback |
| **Gift visual composer** | Procedurální fallback když chybí mood PNG | OK dokud běží `restore:koj-sprites` |
| **Solo stream OBS** | Env scény (`SPINAK_AFK`…) musí existovat v OBS | Malý — doplnit `.env` |
| **LLM hybrid** | Volitelný, potřebuje API key | OK |
| **Duel sync** | `MIA_DUEL_SYNC_ENABLED=false` default | OK pro single stream |
| **Share bridge** | Vypnuto default | OK |

### 🟢 Hotové / nechat

- Text banka (`text-bank/packs/`) — modulární, funguje
- Testy v `npm run test:smoke` — regrese pipeline
- `config/solo-stream.json` — škálovatelná konfigurace
- Redirect overlaye (`mia-overlay.html` → voice)

---

## 3. npm skripty — co je k čemu

| Skript | Verdict |
|--------|---------|
| `restore:koj-sprites` | ✅ **Používej** |
| `generate:koj-sprites` | ⚠️ Matoucí alias — stejné jako restore |
| `generate:mega-bank*` | ❌ **Odstranit** z package.json |
| `test:mega-bank` | ❌ Odstranit s mega bankou |
| `generate:animation-bank` | 🟡 Dev fallback, ne produkce |
| `test:smoke` | ✅ Hlavní CI lokálně |
| `test:solo-stream`, `test:story-animation`, `test:mia-eyes` | ✅ Feature testy |

---

## 4. Velikost / odlehčení

| Co | Odhad | Akce |
|----|-------|------|
| `mega/` | ~37 MB | Smazat |
| Mrtvé `.js` | ~200 KB | Archiv `archive/deprecated/` |
| `logs/` | rostou | Rotace už běží (7 dní) |
| `node_modules/` | normální | `.gitignore` |

**Po vyčištění mega:** −37 MB, méně zmatku v assetech.

---

## 5. Plán dokončení (doporučené pořadí)

1. **Archiv mrtvého kódu** → `archive/deprecated/` (bez mazání SUPPORT_RESOLVER)
2. **Smazat `mega/`** assets
3. **Upravit `package.json`** — pryč mega skripty
4. **Sloučit kontrakty** share bridge → `shared/platform_runtime_contracts`
5. **OBS `.env`** — solo stream scény + ověřit split overlaye
6. **Volitelně:** greeting aggregator zapojit do shadow nebo definitivně pryč

---

## 6. Co NEDĚLAT

- ❌ Spouštět `generate:mega-bank` — zelený procedurální Koj
- ❌ Používat `mia-live-hub.html` v OBS (split 5 zdrojů)
- ❌ `MIA_INGEST_ROUTER` — mrtvá větev
- ❌ Duplicitní `MIA_NEXT/decision` místo `shared/`

---

## 7. Rychlá diagnostika po úklidu

```bash
npm run test:smoke
npm run test:solo-stream
npm run test:story-animation
npm run restore:koj-sprites
curl http://127.0.0.1:3000/status
curl http://127.0.0.1:3000/solo-stream/status
```

---

*Vygenerováno audit pass 18. 6. 2026. Archivace kódu: viz `archive/deprecated/README.md`.*
