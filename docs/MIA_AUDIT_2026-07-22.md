# MIA Audit — 2026-07-22

**Scope:** Push attempt + re-run `node --check` · phase1–4 · `test:preflight:fast` · health/admin probe  
**Prior audit:** [`MIA_AUDIT_2026-07-21_EVENING.md`](./MIA_AUDIT_2026-07-21_EVENING.md) (~94 %)  
**Local HEAD:** `31122636` — refactor: koj runtime split phase F — stage mood/wander engine

---

## 1. Verdikt

**Automatika zelena** — syntax + phase1–4 + preflight:fast vsechny OK.  
**Push neprobehl** — `origin` = `https://github.com/vrtyland/MIA.git`, vetev `master` bez upstream; Git Credential Manager nema ulozene prihlaseni a v agent shellu nelze interaktivne zadat username (fatal: could not read Username).  
**Server** — `/health` a `/admin/status` na `:3000` nedostupne (proces nebezi).

**Stream-ready automatika:** ~94 % (beze zmeny od evening auditu; operatorsky Preview/AQ/mute + uspesny push stale open).

---

## 2. Push

| Polozka | Vysledek |
|---------|----------|
| Branch | `master` (no upstream) |
| Remote | `https://github.com/vrtyland/MIA.git` |
| Command | `git push -u origin HEAD` |
| Vysledek | **FAIL** — auth / GCM hang → non-interactive: `could not read Username for 'https://github.com'` |
| Force | ne |

**Dalsi krok pro cloveka:** v beznem terminalu (ne agent) prihlasit GitHub (GCM dialog / `git credential-manager github login`) a znovu `git push -u origin HEAD`.

---

## 3. Testy (2026-07-22)

| Suite | Exit | Poznamka |
|-------|------|----------|
| `node --check index.js` | 0 | OK |
| `npm run test:phase1` | 0 | normalizer, AQ, runtime-state, watchdog, replay |
| `npm run test:phase2` | 0 | director, combo, viewer-memory, storyboard, theme |
| `npm run test:phase3` | 0 | game layer |
| `npm run test:phase4` | 0 | product boundary |
| `npm run test:preflight:fast` | 0 | vcetne sprint_* / graphics / paint (retry netreba) |
| `GET :3000/health` | down | server nebezi |
| `GET :3000/admin/status` | down | server nebezi |

---

## 4. Dirty tree (nezmeneno v tomto behu)

Stale velke necommitovane kbeliky: `_canon_import/`, `shared/mia-*-core/`, `tests/mia_master_canon_*`, live `data/**`, export docs. **Nekommitovat `data/**`.** Push by stejne odeslal jen uz commitnutou historii (HEAD `31122636`).

---

## 5. Open items

1. Uspesny `git push -u origin HEAD` po GCM login  
2. Live health/admin kdyz bezi server  
3. Canon dirty → oddeleny C6canon commit (az bude plan)